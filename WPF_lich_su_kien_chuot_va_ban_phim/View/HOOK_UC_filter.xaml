<UserControl x:Class="WPF_lich_su_kien_chuot_va_ban_phim.View.HOOK_UC_filter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F1F5F9">

    <UserControl.Resources>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,1,0" Padding="20">
            <StackPanel>
                <TextBlock Text="Cấu Hình Lọc" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>

                <TextBlock Text="Tìm kiếm hành động" FontSize="14" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,5"/>
                <Border CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" Padding="5" Background="#F8FAFC" Margin="0,0,0,20">
                    <TextBox x:Name="txtSearch" Padding="5" Background="Transparent" BorderThickness="0" FontSize="14" Foreground="#334155" />
                </Border>

                <TextBlock Text="Loại thiết bị" FontSize="14" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,10"/>
                <StackPanel>
                    <CheckBox x:Name="cbMouse" Content="Mouse (Chuột)" IsChecked="True" Margin="0,5" Foreground="#334155" FontSize="14"/>
                    <CheckBox x:Name="cbKeyboard" Content="Keyboard (Phím)" IsChecked="True" Margin="0,5" Foreground="#334155" FontSize="14"/>
                    <CheckBox x:Name="cbOnlyCombo" Content="🔥 Chỉ hiện Combo" Foreground="#DC2626" FontWeight="Bold" Margin="0,10,0,5" FontSize="14"/>
                </StackPanel>

                <Separator Margin="0,20" Background="#E2E8F0"/>

                <Button Content="Refresh" Click="BtnRefresh_Click" Margin="0,30,0,0" Height="40" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button> 
                <Button x:Name="BtnShowReport" 
                    Content="📊 Xem Thống Kê" 
                    Click="BtnShowReport_Click"
                    Height="35" 
                    Margin="0,10,0,0" 
                    Background="#FF2196F3" 
                    Foreground="White" 
                    FontWeight="Bold"
                    BorderThickness="0">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="Nhật Ký Sự Kiện" FontSize="22" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                <Border Background="#DBEAFE" CornerRadius="10" Padding="10,2" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="lblCount" Text="0 sự kiện" Foreground="#2563EB" FontSize="12" FontWeight="SemiBold"/>
                </Border>
                
                
            </StackPanel>

            <ListBox x:Name="lstEvents" Grid.Row="1" 
                     Background="Transparent" BorderThickness="0" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="10" Padding="15,10" Margin="0,0,0,10" BorderBrush="#E2E8F0" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="40" Height="40" Background="{Binding IconBgColor}" CornerRadius="8" Margin="0,0,15,0">
                                    <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DecodedAction}" FontWeight="Bold" Foreground="{Binding TextColor}" FontSize="15"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding SourceFile}" Foreground="#94A3B8" FontSize="12" Margin="0,0,10,0"/>
                                        <TextBlock Text="•" Foreground="#CBD5E1" FontSize="12" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding DisplayTime}" Foreground="#64748B" FontSize="12" FontStyle="Italic"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>
    </Grid>
</UserControl>